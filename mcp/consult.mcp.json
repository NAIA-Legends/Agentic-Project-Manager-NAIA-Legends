{
  "//": "Domain MCP servers for NAIA Legends consultants. 'unreal' is the editor-embedded UE 5.8 MCP (native + VibeUE) — only reachable where an editor session runs (Brian's machine / future self-hosted runner); elsewhere it's marked failed and consults proceed from code+docs+web. 'github' exists solely for QA's scoped type:bug write exception.",
  "mcpServers": {
    "unreal": {
      "type": "http",
      "url": "${UNREAL_MCP_URL:-http://127.0.0.1:8000/mcp}"
    },
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer ${GITHUB_MCP_TOKEN}"
      }
    }
  }
}
